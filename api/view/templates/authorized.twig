{% extends 'layouts/main.twig' %}

{% block content %}
    <h1>Projects</h1>

    <ul>
        {% for project in projects %}
            <li>
                <a href="/projects/{{ project.id }}/detail">{{ project.name }}</a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}
